tags: [develop]
public: true
serial: true
plan:
- aggregate:
  - get: concourse
    passed: [dev-image]
    trigger: true
  - get: dev-image
    passed: [dev-image]
    params: {format: oci}
  - get: concourse-image
    params: {format: oci}
  - get: postgres-image
    params: {format: oci}
  - get: unit-image
    passed: [dev-image]
- task: downgrade-test
  privileged: true
  image: unit-image
  file: concourse/ci/tasks/downgrade-test.yml
