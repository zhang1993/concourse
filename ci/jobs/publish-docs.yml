tags: [publish]
public: true
serial: true
plan:
- aggregate:
  - get: version
    passed: [shipit]
    trigger: true
  - get: docs
- task: build-docs
  file: docs/ci/build.yml
  params: {ANALYTICS_ID: ((analytics_id))}
- put: docs-gh-pages
  params: {repository: built-docs}
