---
platform: linux

image_resource:
  type: registry-image
  source: {repository: concourse/unit}

inputs:
- name: concourse

run:
  dir: concourse
  path: bash
  args:
  - -c
  - |
    stty cols 80
    yarn install
    yarn spec
